from setuptools import find_packages, setup

setup(
    name='mve',
    version='0.0.1',
    description="Model-based value expansion",
    platforms=['any'],
    url="https://github.com/TODO",
    packages=find_packages(),
    install_requires=[
        'numpy>=1<2',
    ]
)
