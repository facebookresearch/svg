env_name: gym_fswimmer
num_train_steps: 5e4
eval_freq: 10000

# Environment-specific overrides:
learn_temp:
  params:
    init_targ_entr: 0
    final_targ_entr: -16
    entr_decay_factor: 0.125

agent:
  params:
    horizon: 5
    warmup_steps: 1000
